import 'package:flutter/material.dart';
import 'package:tracking_user/widgets/sav/textfield_add_sav_widget.dart';

class SavWidget extends StatelessWidget {
  const SavWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: const [
      TextfieldSavWidget()

      ],
    );
  }
}